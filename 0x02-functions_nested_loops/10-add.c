#include "main.h"

/**
 * add - adds two integers and gives result
 * @a: first number to add
 * @b: second number to add
 * Return: result of sum of integers
 */

int add(int a, int b)
{
	int i, j, sum;

	i = a;
	j = b;
	sum = i + j;

	return (sum);
}
